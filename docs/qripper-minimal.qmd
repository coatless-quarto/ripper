---
title: "Minimal Example"
format: html
filters:
  - ripper
---

This example shows the extension's default behavior.

```{r}
#| label: simple-calculation
x <- 1:10
mean(x)
```

```{python}
#| label: python-calculation
numbers = list(range(1, 11))
print(sum(numbers) / len(numbers))
```

